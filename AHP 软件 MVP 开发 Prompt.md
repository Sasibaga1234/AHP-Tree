# AHP Decision Tool — MVP 开发 Prompt

你需要开发一个基于 Web 的 AHP（Analytic Hierarchy Process，层次分析法）决策分析工具。

## 一、产品目标

这是一个面向**已经知道或正在使用 AHP 的用户**的工具。

不要把产品设计成 AI 问答助手，也不要通过大量简单问题引导用户描述需求。

核心流程非常明确：

**设计 AHP 层次结构 → 输入两两比较 → 计算 → 查看结果**

第一版的目标是把这个核心流程做得简单、直观、可靠。

暂时不要实现：

- 数学公式动画
- 教学课程
- 数据库
- 复杂后端

这些属于后续版本。
---

# 二、技术栈

使用：

- Vue 3
- TypeScript
- Vite
- Tailwind CSS
- KaTeX（如果需要显示数学公式）
- Lucide Icons 或其他轻量图标库

第一版尽量做成**纯前端应用**。

AHP 的数学计算必须独立于 Vue UI。

建议结构：

```text
src/
├── components/
│   ├── TreeEditor.vue
│   ├── ComparisonScale.vue
│   ├── ComparisonMatrix.vue
│   └── ResultView.vue
│
├── ahp/
│   ├── matrix.ts
│   ├── priority.ts
│   ├── consistency.ts
│   └── ahp.ts
│
├── types/
│   └── ahp.ts
│
└── App.vue
```

Vue 负责 UI、交互和状态。

AHP Engine 负责纯数学计算。

不要把 AHP 计算逻辑直接写死在 Vue component 中。

---

# 三、核心数据结构

AHP 模型应该使用树状结构。

例如：

```text
选择汽车
├── 价格
├── 性能
└── 舒适度
```

或者：

```text
选择汽车
├── 经济性
│   ├── 价格
│   └── 油耗
├── 性能
│   ├── 动力
│   └── 操控
└── 舒适性
    ├── 空间
    └── 座椅
```

节点至少包含：

```typescript
type Node = {
  id: string
  name: string
  children: Node[]
}
```

需要支持：

- 修改节点名称
- 添加子节点
- 删除节点
- 添加多个层级
- 显示树状关系

第一版不需要实现复杂的拖拽排序。

---

# 四、页面 / 工作流程

整个 MVP 采用三个主要阶段：

## 1. Structure — 设计树

进入软件后直接显示一个默认 AHP 模板，例如：

```text
                 汽车选择
                    │
        ┌───────────┼───────────┐
        │           │           │
       价格         性能        舒适度
```

用户可以直接修改：

```text
汽车选择 → 选择笔记本
价格 → 价格
性能 → 性能
舒适度 → 便携性
```

并可以添加、删除节点。

界面应该强调“编辑决策层次结构”，而不是让用户回答“你的目标是什么”之类的问题。

---

# 五、Comparison — 两两比较

完成树结构后进入比较阶段。

对于每一个需要比较的兄弟节点，进行两两比较。

提供两种输入方式：

## 模式 A：1–9 双向评分

用户可以自行选择 Saaty 1–9 标度。

界面使用一个水平的双向选择条。

例如：

```text
价格更重要                         性能更重要

  9     7     5     3     1     3     5     7     9
  ■     ■     ■     ■     ●     ■     ■     ■     ■
```

中间的 `1` 表示：

**两者同等重要**

左侧表示第一个对象更重要。

右侧表示第二个对象更重要。

用户不应该需要手动输入数字。

点击某个区块后，立即记录对应的 AHP 判断值。

例如：

```text
价格 ← 5 → 性能
```

内部保存：

```text
price / performance = 5
performance / price = 1/5
```

建议使用：

```text
1/9, 1/7, 1/5, 1/3, 1,
3, 5, 7, 9
```

作为实际判断值。

不要简单地把左右两侧保存成 `-9 ~ +9`。

---

## 模式 B：判断矩阵

用户也可以直接编辑判断矩阵。

例如三个指标：

```text
          价格    性能    舒适度

价格       1       ?       ?
性能       ?       1       ?
舒适度     ?       ?       1
```

用户输入一个值后，对称位置自动填写倒数。

例如：

```text
价格 / 性能 = 1/5
```

自动变成：

```text
性能 / 价格 = 5
```

矩阵对角线始终自动设置为：

```text
1
```

并且不可编辑。

输入框应该允许：

```text
1
3
5
1/3
1/5
0.2
```

等形式。

需要有可靠的数值解析器，将例如 `1/3` 转换为数值。

---

# 六、AHP 数学计算

AHP Engine 必须独立实现，并且计算结果应该是确定性的。

至少支持：

1. 判断矩阵
2. 权重计算
3. 最大特征值 λmax
4. CI（Consistency Index）
5. CR（Consistency Ratio）
6. 局部权重
7. 层次总排序 / 全局权重
8. 最终方案排序

对于权重计算，可以先采用稳定、容易解释的标准方法。

需要明确记录计算过程，为未来加入数学过程可视化留下接口。

例如：

```typescript
type AHPResult = {
  matrix: number[][]
  priorityVector: number[]
  lambdaMax: number
  consistencyIndex: number
  consistencyRatio: number
  globalWeights: Record<string, number>
  ranking: RankingItem[]
}
```

---

# 七、结果页面

计算完成后直接显示结果。

至少包含：

## 指标权重

例如：

```text
性能       48.2%
价格       31.4%
舒适度     20.4%
```

## 最终方案排序

例如：

```text
1. Toyota     42.1%
2. Honda      35.6%
3. Mazda      22.3%
```

## 一致性

例如：

```text
Consistency Ratio

CR = 0.064

✓ 判断具有可接受的一致性
```

如果 CR 超过常用阈值，应明显提醒用户：

```text
⚠ 判断一致性较低，建议重新检查比较结果。
```

不要只显示一个数字。

---

# 八、UI / UX 原则

整体设计应该：

- 简洁
- 清晰
- 专业
- 有现代感
- 不要过度装饰
- 不要出现大量 AI 聊天气泡
- 不要让用户在页面中迷失

核心操作应该始终非常明显：

```text
① 设计树
      ↓
② 输入比较
      ↓
③ 查看结果
```

可以使用顶部步骤导航：

```text
Structure  →  Comparison  →  Results
```

让用户明确自己现在处于哪个阶段。

---

# 九、重要的架构原则

AHP 数学引擎和 UI 必须分离。

例如：

```text
Vue UI
   ↓
用户输入
   ↓
AHP Model
   ↓
AHP Engine
   ↓
AHP Result
   ↓
Vue UI
```

不要让组件自己计算：

```text
CR
λmax
权重
矩阵
```

这些都应该由 `src/ahp/` 中的纯 TypeScript 函数完成。

这样未来可以很容易增加：

- 数学公式展示
- 数学计算动画
- AHP 教学模式
- sensitivity analysis
- 更多 AHP 方法
- 导出功能

---

# 十、未来扩展，但现在不要实现

未来计划加入一个 Learning / Math 模式。

例如：

```text
3x - 5 = 13
```

可以通过动画展示：

```text
3x - 5 + 5 = 13 + 5
```

最终：

```text
3x = 18
```

以及 AHP 中：

```text
判断矩阵
↓
归一化
↓
权重
↓
λmax
↓
CI
↓
CR
```

但是**当前 MVP 不实现这些动画**。

因此当前代码应该保持足够模块化，为未来的数学可视化留下接口，但不要为了未来功能过度设计。

---

# 十一、开发原则

开发时遵守以下原则：

1. 先完成完整的 Structure → Comparison → Results 流程。
2. 不要自行添加没有要求的复杂功能。
3. 不要加入 AI 功能。
4. 不要加入登录和数据库。
5. 不要为了展示技术而使用复杂框架。
6. AHP 数学计算必须准确。
7. UI 必须可以实际完成一次完整的 AHP 分析。
8. 所有核心状态应该可以序列化为 JSON，方便未来保存和导出。
9. 数学计算代码必须有清晰的函数边界。
10. 优先完成可运行的 MVP，而不是提前实现未来功能。

最终目标不是制作一个“功能很多”的软件，而是制作一个：

> **让 AHP 用户可以快速建立层次结构、进行两两比较、得到可靠计算结果的简洁 Web 工具。**